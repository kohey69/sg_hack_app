-# locals(food:)

= simple_form_for [:admins, food] do |f|
  .form-inputs
    = f.input :name
    = f.input :description, as: :text
    = f.input :refrigerated

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
